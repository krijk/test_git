name: test_git
description: "A github Flutter project."
publish_to: 'none'

version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  faker: ^2.1.0
  flutter:
    sdk: flutter
  flutter_fancy_tree_view: ^1.4.1
  flutter_markdown: ^0.6.18
  mylib01:
    git:
      url: https://github.com/krijk/mylib01.git
  package_info_plus: ^5.0.1
  provider: ^6.1.1
  settings_ui: ^2.0.2

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
     - assets/docs/


